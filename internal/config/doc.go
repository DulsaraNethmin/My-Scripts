// Package config owns everything under ~/.spinup: config.yaml, the
// materialised stacks in stacks/ and the per-stack env files in env/.
//
// Filled in by task 2.3: path resolution per OS, defaults, and merging a
// stack's .env.example with the user's env file.
package config
