// Package docker inspects the local Docker installation: whether the daemon is
// running, whether Compose v2 is present, which host ports are taken and
// whether an NVIDIA runtime is available.
//
// Filled in by task 2.7, which is also where `spinup doctor` gets its checks.
package docker
