# stacks/nats — copy to ~/.spinup/env/nats.env and edit as needed.
# Every value here is the default baked into compose.yaml; uncomment to override.

# Host port clients connect to.
NATS_PORT=4222

# Host port for the monitoring endpoints (/varz, /connz, /healthz).
NATS_MONITOR_PORT=8088
